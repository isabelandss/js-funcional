/**
 * Diz-se que uma linguagem de programação possui
 * funções de primeira classe quando funções nessa 
 * linguagem são tratadas como qualquer variável
 * (resumindo: é possível armazenar funções em variáveis)
 */

const fn = function() {}
const fn2 = () => {}